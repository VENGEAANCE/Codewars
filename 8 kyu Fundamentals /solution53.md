# Task

Remove white spaces from the given string

# Solution

```ruby
function noSpace(x){
  
  let solution = x.split(' ').join('');
  
  return solution;
}

```

# Explanation

I used the split variable to remove sapce pattern, then joined the string without space inbetween. 